
module.exports = {

    manager: {
        serverLocation: String
    },

    logging: {
        logDebug: Boolean,
        logDetail: Boolean,
        logGeneral: Boolean,
        logWarning: Boolean,
        logError: Boolean,
        prefix: String,
        writeLog: Boolean,
        fileName: String,
        filePath: String,
        fileSize: String,
        fileAge: Number,
        fileCount: Number
    }

}